n по сжатому режиму note
пкм по адресу сдвинуть (добавить N байт) + сдвиг таблицы и обнова хедера relocTable size datasize и тд // результат у нас новый блок 0 начина€ с выделенного байта и по написанный size
пкм по адресу import bin -> потом подокно load size и уже заполненный размер файла (поле редактировани€) и галочка shiftData for bin (режим выше, добавить байты)б если нет add то обычна€ замена
редактирование: преключать на след байты (зажимаешь F пишет в даль как cheatengine)
выделенные байты пкм копировать - копируем как строка
ctrl v на байте и у нас в буфере строка байт - вставл€емб если ctrl v и у нас выделенный размер пишем с буффера сколько влезет в выделение (buff trim)
save json/dtz раздельно? сохран€ть настры не сжим€€ заново dtz
параметр при сжатии Save as x86 / x64 (сжати€ расжатие структуры под оффсеты), см мой loadchunk compat
выделенные байты пкм експорт .bin
Edit val как cheatenfine fload double hex dec
экспорт GraphicalView фиол - 0xAAAAA 0xDDDD 0xFFFFFF последовательности 8+ красные pointers, headers, tables, синий рандомные данные (save as png)
добавить байты в конце расширить файл??? (useless тк комманда пкм по адресу сдвинуть (добавить N байт)  круче и полезнее)